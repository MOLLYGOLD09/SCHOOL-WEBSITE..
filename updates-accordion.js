(function(){const items=document.querySelectorAll('#updatesAccordion .ua-item');items.forEach((item)=>{const btn=item.querySelector('.ua-header');btn.addEventListener('click',()=>{const open=item.classList.contains('is-open');items.forEach(it=>{it.classList.remove('is-open');const b=it.querySelector('.ua-header');b&&b.setAttribute('aria-expanded','false');});item.classList.toggle('is-open',!open);btn.setAttribute('aria-expanded',String(!open));});});})();